#include <ctype.h>
#include "main.h"

/**
 * _isalpha - checks if a character is alpha
 * @c: character to test
 * Return: 1 if c is a letter, lowercase or uppercase, 0 otherwise
 */
int _isalpha(int c)
{
	if (isalpha(c))
	{
		return (1);
	}
	return (0);
}
