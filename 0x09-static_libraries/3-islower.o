#include <ctype.h>
#include "main.h"

/**
 * _islower - checks if a character is lower case
 * @c: character to test
 *
 * Return: 1 if lower, 0 if upper
 */
int _islower(int c)
{
	if (islower(c))
	{
		return (1);
	}
	return (0);
}
