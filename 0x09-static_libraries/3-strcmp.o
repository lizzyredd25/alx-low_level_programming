#include <string.h>
/**
 * _strcmp - copies a strings.
 * @s1: string  1
 * @s2: string 2=
 * Return: pointer to the resulting string dest
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
