#include <string.h>
#include <stdio.h>
/**
 * set_string - sets the value of a pointer to a character
 * @s: array of square matrix
 * @to: size of array a
 * Return: 0
 */
void set_string(char **s, char *to)
{
	*s = to;
}
